<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Build Your Website, Database Mannagement System, and Blogs</title>
    <meta name="keywords" content="Website, Blog, Database">
    <meta name="description" content="We build  websites, and Blogs, Responsive and affordable.">

    <link rel="stylesheet" href="assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="assets/flat-icon/flaticon.css">
    <link rel="stylesheet" href="temp/styles/styles.css">
    <link href="assets/font-awesome/css/font-awesome.css" rel="stylesheet">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>

    <div class="main-wrapper">

        <!--SERVICES SESSION-->
        <% include contents/header.ejs %>

            <!--SERVICES SESSION-->
            <% include contents/services.ejs %>

                <!--ABOUT SESSION-->

                <% include contents/about.ejs %>

                    <!--DESIGN TEMPLATES-->

                    <% include contents/designs.ejs %>

                        <!--VIDEO SESSION-->
                        <% include contents/videos.ejs %>

                            <!--TEAM MEMBERS-->
                            <% include contents/team.ejs %>

                                <!--PRICE SUPORT-->
                                <% include contents/price.ejs %>

                                    <!--PROJECTS SESSION-->
                                    <% include contents/projects.ejs %>

                                        <!--TESTIMONIALS SESSION-->
                                        <% include contents/testimonies.ejs%>

                                            <!--CONTACT SESSION-->
                                            <% include contents/contacts.ejs %>

                                                <!--BLOG SESSION-->
                                                <% include contents/blogs.ejs %>

                                                    <!--FOOTER SESSION-->
                                                    <% include contents/footer.ejs%>


    </div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="assets/jquery/jquery-3.2.1.min.js"></script>
    <script src="assets/bootstrap/js/bootstrap.min.js"></script>

    <script>
        $(document).ready(function() {
            // Add scrollspy to <body>
            $('body').scrollspy({
                target: ".navbar",
                offset: 50
            });

            // Add smooth scrolling on all links inside the navbar
            $("#myNavbar a").on('click', function(event) {
                // Make sure this.hash has a value before overriding default behavior
                if (this.hash !== "") {
                    // Prevent default anchor click behavior
                    event.preventDefault();

                    // Store hash
                    var hash = this.hash;

                    // Using jQuery's animate() method to add smooth page scroll
                    // The optional number (800) specifies the number of milliseconds it takes to scroll to the specified area
                    $('html, body').animate({
                        scrollTop: $(hash).offset().top
                    }, 800, function() {

                        // Add hash (#) to URL when done scrolling (default click behavior)
                        window.location.hash = hash;
                    });
                } // End if
            });
        });
    </script>
    <script>
        $(document).ready(function() {

            var $videoSrc = $("#video").attr("src");

            $('#myModal').on('hide.bs.modal', function(e) {
                $("#video").attr('src', $videoSrc);
            });



        });
    </script>


</body>

</html>